#include <conio.h>
#include <stdio.h>
int main()
{
    int a, b;
    a = 34;
    b = 0;
    // in c programing 0 consider false 1 consider true
    printf("!b = %d\n", !b);
    printf("a||b = %d\n", a || b);// "||" indicate if one of the condtion is true then answer is true  
    printf("a&&b = %d\n", a && b);// "&&" indicate both condition must be true then answer is true
    printf("a+b = %d\n", a + b);
    printf("a*b= %d\n", a * b);
    printf("b/a= %d\n", b / a);
    return 0;
}